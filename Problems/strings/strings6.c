// While printing the genotype of the F2 generation of Capsella bursa (Shepherd's Purse), Nahar by mistake wrote t1 t1 t2 t2 for two progeny, which was supposed to be for one.
// Help her change t1 t1 t2 t2 to t1 t1 T2 T2 so that only one progeny shows recessive traits.
// For hint type "hint"

#include <string.h>
#include <stdio.h>
int main()
{
    char genotype[1000], recessive, dominant;
    int i;
    // mention the genotype, gene to replace, gene to be added


    for (............) //condition
    {
        
        // replace genes

    }

    printf("after replace:%s\n", genotype);

    return 0;
}